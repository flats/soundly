<%= form_for(@sound) do |f| %>
  <% if @sound.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sound.errors.count, "error") %> prohibited this sound from being saved:</h2>

      <ul>
      <% @sound.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
  <%= f.fields_for :soundfiles do |soundfile_form| %>
    <%= soundfile_form.label :soundfile, "Upload a file" %><br>
    <%= soundfile_form.file_field :soundfile %><br>
  <% end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>